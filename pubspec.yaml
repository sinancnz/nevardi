name: nevardi
description: A minimal pantry tracker ("Ne Vardı?") demo
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    uses-material-design: true
    shared_preferences: ^2.2.2
    intl: ^0.19.0
  uuid: ^4.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0



# Trigger build
